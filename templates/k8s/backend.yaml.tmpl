{{- /* Code generated by sgctl. DO NOT EDIT. */ -}}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: sgroups-backend
  namespace: {{ .Namespace }}
  labels:
    app: sgroups-backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: sgroups-backend
  template:
    metadata:
      labels:
        app: sgroups-backend
    spec:
      initContainers:
        - name: goose-migrate
          image: sgroups-migration:latest
          imagePullPolicy: IfNotPresent
          env:
            - name: GOOSE_DRIVER
              value: postgres
            - name: GOOSE_DBSTRING
              value: "host=sgroups-postgres user=user_admin password=admin dbname={{ .DBSchema }} sslmode=disable"
      containers:
        - name: backend
          image: sgroups-backend:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 9006
              name: grpc
            - containerPort: 9010
              name: health
          env:
            - name: DB_HOST
              value: sgroups-postgres
            - name: DB_PORT
              value: "5432"
            - name: DB_USER
              value: user_admin
            - name: DB_PASSWORD
              value: admin
            - name: DB_NAME
              value: "{{ .DBSchema }}"
          readinessProbe:
            httpGet:
              path: /healthz
              port: 9010
            initialDelaySeconds: 3
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /healthz
              port: 9010
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: sgroups-backend
  namespace: {{ .Namespace }}
spec:
  selector:
    app: sgroups-backend
  ports:
    - name: grpc
      port: 9006
      targetPort: 9006
