-- DO NOT EDIT: generated by sgctl
-- Views for composite resource: {{ .Res.Name }}

{{ range .Res.Subtypes }}
CREATE OR REPLACE VIEW {{ $.Schema }}.vu_{{ .Table }} AS
SELECT
    t.uid,
    t.name,
    t.namespace,
    t.display_name,
    t.description,
    t.comment,
    t.labels,
    t.annotations,
    t.resource_version,
    t.creation_timestamp
FROM {{ $.Schema }}.tbl_{{ .Table }} t;

{{ end }}
