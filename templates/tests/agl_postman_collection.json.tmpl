{{- /* DO NOT EDIT: generated by sgctl */ -}}
{{- /* Postman v2.1 collection — AGL (K8s Aggregated API) tests for all resources from schema.yaml */ -}}
{{- /* Mirrors backend postman_collection.json tests but through K8s API paths */ -}}
{{- $schema := . -}}
{
	"info": {
		"name": "Generated AGL Tests (K8s API)",
		"schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
	},
	"item": [
{{- $first := true }}
{{- range $ri, $res := .Resources }}
{{- if not $res.IsComposite }}
{{- $plural := k8sPlural $res.Name }}
{{- $singular := k8sSingular $res.Name }}
{{- $kebabName := kebab $res.Name }}
{{- if not $first }},{{ end }}{{ $first = false }}
		{
			"name": "{{ $res.Name }} — LIST",
			"item": [
				{
					"name": "Поиск всех {{ $plural }}",
					"request": {
						"method": "GET",
						"header": [],
						"url": {
							"raw": "{{`{{k8s_url}}`}}/apis/{{ $.Project.APIGroup }}/{{ $.Project.APIVersion }}/{{ $plural }}",
							"host": ["{{`{{k8s_url}}`}}"],
							"path": ["apis", "{{ $.Project.APIGroup }}", "v1alpha1", "{{ $plural }}"]
						}
					},
					"event": [{
						"listen": "test",
						"script": {
							"type": "text/javascript",
							"exec": [
								"pm.test('Status code is 200', () => { pm.response.to.have.status(200); });",
								"pm.test('Response has items array', () => {",
								"    const body = pm.response.json();",
								"    pm.expect(body).to.have.property('items');",
								"    pm.expect(body.items).to.be.an('array');",
								"});",
								"pm.test('Kind is {{ $res.Name }}List', () => {",
								"    const body = pm.response.json();",
								"    pm.expect(body.kind).to.eql('{{ $res.Name }}List');",
								"});"
							]
						}
					}]
				}
{{- if $res.IsNamespaced }},
				{
					"name": "Поиск по namespace",
					"request": {
						"method": "GET",
						"header": [],
						"url": {
							"raw": "{{`{{k8s_url}}`}}/apis/{{ $.Project.APIGroup }}/{{ $.Project.APIVersion }}/namespaces/{{`{{test_namespace}}`}}/{{ $plural }}",
							"host": ["{{`{{k8s_url}}`}}"],
							"path": ["apis", "{{ $.Project.APIGroup }}", "v1alpha1", "namespaces", "{{`{{test_namespace}}`}}", "{{ $plural }}"]
						}
					},
					"event": [{
						"listen": "test",
						"script": {
							"type": "text/javascript",
							"exec": [
								"pm.test('Status code is 200', () => { pm.response.to.have.status(200); });",
								"pm.test('All items in correct namespace', () => {",
								"    const body = pm.response.json();",
								"    body.items.forEach(item => {",
								"        pm.expect(item.metadata.namespace).to.eql(pm.variables.get('test_namespace'));",
								"    });",
								"});"
							]
						}
					}]
				}
{{- end }},
				{
					"name": "Поиск по labels",
					"request": {
						"method": "GET",
						"header": [],
						"url": {
							"raw": "{{`{{k8s_url}}`}}/apis/{{ $.Project.APIGroup }}/{{ $.Project.APIVersion }}/{{ $plural }}?labelSelector=env%3Dtest",
							"host": ["{{`{{k8s_url}}`}}"],
							"path": ["apis", "{{ $.Project.APIGroup }}", "v1alpha1", "{{ $plural }}"],
							"query": [{"key": "labelSelector", "value": "env=test"}]
						}
					},
					"event": [{
						"listen": "test",
						"script": {
							"type": "text/javascript",
							"exec": [
								"pm.test('Status code is 200', () => { pm.response.to.have.status(200); });",
								"pm.test('Response is a list', () => {",
								"    const body = pm.response.json();",
								"    pm.expect(body.kind).to.eql('{{ $res.Name }}List');",
								"});"
							]
						}
					}]
				},
				{
					"name": "GET несуществующий {{ $singular }}",
					"request": {
						"method": "GET",
						"header": [],
						"url": {
{{- if $res.IsNamespaced }}
							"raw": "{{`{{k8s_url}}`}}/apis/{{ $.Project.APIGroup }}/{{ $.Project.APIVersion }}/namespaces/{{`{{test_namespace}}`}}/{{ $plural }}/nonexistent-{{ $kebabName }}",
							"host": ["{{`{{k8s_url}}`}}"],
							"path": ["apis", "{{ $.Project.APIGroup }}", "v1alpha1", "namespaces", "{{`{{test_namespace}}`}}", "{{ $plural }}", "nonexistent-{{ $kebabName }}"]
{{- else }}
							"raw": "{{`{{k8s_url}}`}}/apis/{{ $.Project.APIGroup }}/{{ $.Project.APIVersion }}/{{ $plural }}/nonexistent-{{ $kebabName }}",
							"host": ["{{`{{k8s_url}}`}}"],
							"path": ["apis", "{{ $.Project.APIGroup }}", "v1alpha1", "{{ $plural }}", "nonexistent-{{ $kebabName }}"]
{{- end }}
						}
					},
					"event": [{
						"listen": "test",
						"script": {
							"type": "text/javascript",
							"exec": [
								"pm.test('Status code is 404', () => { pm.response.to.have.status(404); });"
							]
						}
					}]
				}
			]
		},
		{
			"name": "{{ $res.Name }} — CREATE / UPDATE",
			"item": [
				{
					"name": "Добавление нового {{ $singular }}",
					"request": {
						"method": "POST",
						"header": [{"key": "Content-Type", "value": "application/json"}],
						"body": {"mode": "raw", "raw": "{\"apiVersion\":\"{{ $.Project.APIGroup }}/{{ $.Project.APIVersion }}\",\"kind\":\"{{ $res.Name }}\",\"metadata\":{\"name\":\"{{ $kebabName }}-new\"{{ if $res.IsNamespaced }},\"namespace\":\"{{`{{test_namespace}}`}}\"{{ end }}},\"spec\":{\"displayName\":\"New {{ $res.Name }}\"{{ testSpecJSON $res }}}}"},
						"url": {
{{- if $res.IsNamespaced }}
							"raw": "{{`{{k8s_url}}`}}/apis/{{ $.Project.APIGroup }}/{{ $.Project.APIVersion }}/namespaces/{{`{{test_namespace}}`}}/{{ $plural }}",
							"host": ["{{`{{k8s_url}}`}}"],
							"path": ["apis", "{{ $.Project.APIGroup }}", "v1alpha1", "namespaces", "{{`{{test_namespace}}`}}", "{{ $plural }}"]
{{- else }}
							"raw": "{{`{{k8s_url}}`}}/apis/{{ $.Project.APIGroup }}/{{ $.Project.APIVersion }}/{{ $plural }}",
							"host": ["{{`{{k8s_url}}`}}"],
							"path": ["apis", "{{ $.Project.APIGroup }}", "v1alpha1", "{{ $plural }}"]
{{- end }}
						}
					},
					"event": [{
						"listen": "test",
						"script": {
							"type": "text/javascript",
							"exec": [
								"pm.test('Status code is 201', () => { pm.response.to.have.status(201); });",
								"pm.test('Created object has correct name', () => {",
								"    const body = pm.response.json();",
								"    pm.expect(body.metadata.name).to.eql('{{ $kebabName }}-new');",
								"});",
								"if (pm.response.code === 201) {",
								"    const body = pm.response.json();",
								"    pm.collectionVariables.set('{{ $kebabName }}_rv', body.metadata.resourceVersion);",
								"}"
							]
						}
					}]
				},
				{
					"name": "GET созданного {{ $singular }}",
					"request": {
						"method": "GET",
						"header": [],
						"url": {
{{- if $res.IsNamespaced }}
							"raw": "{{`{{k8s_url}}`}}/apis/{{ $.Project.APIGroup }}/{{ $.Project.APIVersion }}/namespaces/{{`{{test_namespace}}`}}/{{ $plural }}/{{ $kebabName }}-new",
							"host": ["{{`{{k8s_url}}`}}"],
							"path": ["apis", "{{ $.Project.APIGroup }}", "v1alpha1", "namespaces", "{{`{{test_namespace}}`}}", "{{ $plural }}", "{{ $kebabName }}-new"]
{{- else }}
							"raw": "{{`{{k8s_url}}`}}/apis/{{ $.Project.APIGroup }}/{{ $.Project.APIVersion }}/{{ $plural }}/{{ $kebabName }}-new",
							"host": ["{{`{{k8s_url}}`}}"],
							"path": ["apis", "{{ $.Project.APIGroup }}", "v1alpha1", "{{ $plural }}", "{{ $kebabName }}-new"]
{{- end }}
						}
					},
					"event": [{
						"listen": "test",
						"script": {
							"type": "text/javascript",
							"exec": [
								"pm.test('Status code is 200', () => { pm.response.to.have.status(200); });",
								"pm.test('Object has correct kind and name', () => {",
								"    const body = pm.response.json();",
								"    pm.expect(body.kind).to.eql('{{ $res.Name }}');",
								"    pm.expect(body.metadata.name).to.eql('{{ $kebabName }}-new');",
								"});"
							]
						}
					}]
				},
				{
					"name": "UPDATE {{ $singular }} (displayName)",
					"request": {
						"method": "PUT",
						"header": [{"key": "Content-Type", "value": "application/json"}],
						"body": {"mode": "raw", "raw": "{\"apiVersion\":\"{{ $.Project.APIGroup }}/{{ $.Project.APIVersion }}\",\"kind\":\"{{ $res.Name }}\",\"metadata\":{\"name\":\"{{ $kebabName }}-new\"{{ if $res.IsNamespaced }},\"namespace\":\"{{`{{test_namespace}}`}}\"{{ end }},\"resourceVersion\":\"{{`{{`}}{{ $kebabName }}_rv{{`}}`}}\"},\"spec\":{\"displayName\":\"Updated {{ $res.Name }}\"{{ testSpecJSON $res }}}}"},
						"url": {
{{- if $res.IsNamespaced }}
							"raw": "{{`{{k8s_url}}`}}/apis/{{ $.Project.APIGroup }}/{{ $.Project.APIVersion }}/namespaces/{{`{{test_namespace}}`}}/{{ $plural }}/{{ $kebabName }}-new",
							"host": ["{{`{{k8s_url}}`}}"],
							"path": ["apis", "{{ $.Project.APIGroup }}", "v1alpha1", "namespaces", "{{`{{test_namespace}}`}}", "{{ $plural }}", "{{ $kebabName }}-new"]
{{- else }}
							"raw": "{{`{{k8s_url}}`}}/apis/{{ $.Project.APIGroup }}/{{ $.Project.APIVersion }}/{{ $plural }}/{{ $kebabName }}-new",
							"host": ["{{`{{k8s_url}}`}}"],
							"path": ["apis", "{{ $.Project.APIGroup }}", "v1alpha1", "{{ $plural }}", "{{ $kebabName }}-new"]
{{- end }}
						}
					},
					"event": [{
						"listen": "test",
						"script": {
							"type": "text/javascript",
							"exec": [
								"pm.test('Status code is 200', () => { pm.response.to.have.status(200); });",
								"pm.test('DisplayName updated', () => {",
								"    const body = pm.response.json();",
								"    pm.expect(body.spec.displayName).to.eql('Updated {{ $res.Name }}');",
								"});"
							]
						}
					}]
				},
				{
					"name": "Ошибка при добавлении, name пустая строка",
					"request": {
						"method": "POST",
						"header": [{"key": "Content-Type", "value": "application/json"}],
						"body": {"mode": "raw", "raw": "{\"apiVersion\":\"{{ $.Project.APIGroup }}/{{ $.Project.APIVersion }}\",\"kind\":\"{{ $res.Name }}\",\"metadata\":{\"name\":\"\"{{ if $res.IsNamespaced }},\"namespace\":\"{{`{{test_namespace}}`}}\"{{ end }}},\"spec\":{\"displayName\":\"Invalid\"}}"},
						"url": {
{{- if $res.IsNamespaced }}
							"raw": "{{`{{k8s_url}}`}}/apis/{{ $.Project.APIGroup }}/{{ $.Project.APIVersion }}/namespaces/{{`{{test_namespace}}`}}/{{ $plural }}",
							"host": ["{{`{{k8s_url}}`}}"],
							"path": ["apis", "{{ $.Project.APIGroup }}", "v1alpha1", "namespaces", "{{`{{test_namespace}}`}}", "{{ $plural }}"]
{{- else }}
							"raw": "{{`{{k8s_url}}`}}/apis/{{ $.Project.APIGroup }}/{{ $.Project.APIVersion }}/{{ $plural }}",
							"host": ["{{`{{k8s_url}}`}}"],
							"path": ["apis", "{{ $.Project.APIGroup }}", "v1alpha1", "{{ $plural }}"]
{{- end }}
						}
					},
					"event": [{
						"listen": "test",
						"script": {
							"type": "text/javascript",
							"exec": [
								"pm.test('Status code is 422 or 400', () => {",
								"    pm.expect([400, 422]).to.include(pm.response.code);",
								"});"
							]
						}
					}]
				},
				{
					"name": "Ошибка при добавлении, name из спец.символов",
					"request": {
						"method": "POST",
						"header": [{"key": "Content-Type", "value": "application/json"}],
						"body": {"mode": "raw", "raw": "{\"apiVersion\":\"{{ $.Project.APIGroup }}/{{ $.Project.APIVersion }}\",\"kind\":\"{{ $res.Name }}\",\"metadata\":{\"name\":\"!!!invalid@@@\"{{ if $res.IsNamespaced }},\"namespace\":\"{{`{{test_namespace}}`}}\"{{ end }}},\"spec\":{\"displayName\":\"Invalid\"}}"},
						"url": {
{{- if $res.IsNamespaced }}
							"raw": "{{`{{k8s_url}}`}}/apis/{{ $.Project.APIGroup }}/{{ $.Project.APIVersion }}/namespaces/{{`{{test_namespace}}`}}/{{ $plural }}",
							"host": ["{{`{{k8s_url}}`}}"],
							"path": ["apis", "{{ $.Project.APIGroup }}", "v1alpha1", "namespaces", "{{`{{test_namespace}}`}}", "{{ $plural }}"]
{{- else }}
							"raw": "{{`{{k8s_url}}`}}/apis/{{ $.Project.APIGroup }}/{{ $.Project.APIVersion }}/{{ $plural }}",
							"host": ["{{`{{k8s_url}}`}}"],
							"path": ["apis", "{{ $.Project.APIGroup }}", "v1alpha1", "{{ $plural }}"]
{{- end }}
						}
					},
					"event": [{
						"listen": "test",
						"script": {
							"type": "text/javascript",
							"exec": [
								"pm.test('Status code is 422 or 400', () => {",
								"    pm.expect([400, 422]).to.include(pm.response.code);",
								"});"
							]
						}
					}]
				},
				{
					"name": "Ошибка при добавлении, name > 63 символов",
					"request": {
						"method": "POST",
						"header": [{"key": "Content-Type", "value": "application/json"}],
						"body": {"mode": "raw", "raw": "{\"apiVersion\":\"{{ $.Project.APIGroup }}/{{ $.Project.APIVersion }}\",\"kind\":\"{{ $res.Name }}\",\"metadata\":{\"name\":\"aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa\"{{ if $res.IsNamespaced }},\"namespace\":\"{{`{{test_namespace}}`}}\"{{ end }}},\"spec\":{\"displayName\":\"Invalid\"}}"},
						"url": {
{{- if $res.IsNamespaced }}
							"raw": "{{`{{k8s_url}}`}}/apis/{{ $.Project.APIGroup }}/{{ $.Project.APIVersion }}/namespaces/{{`{{test_namespace}}`}}/{{ $plural }}",
							"host": ["{{`{{k8s_url}}`}}"],
							"path": ["apis", "{{ $.Project.APIGroup }}", "v1alpha1", "namespaces", "{{`{{test_namespace}}`}}", "{{ $plural }}"]
{{- else }}
							"raw": "{{`{{k8s_url}}`}}/apis/{{ $.Project.APIGroup }}/{{ $.Project.APIVersion }}/{{ $plural }}",
							"host": ["{{`{{k8s_url}}`}}"],
							"path": ["apis", "{{ $.Project.APIGroup }}", "v1alpha1", "{{ $plural }}"]
{{- end }}
						}
					},
					"event": [{
						"listen": "test",
						"script": {
							"type": "text/javascript",
							"exec": [
								"pm.test('Status code is 422 or 400', () => {",
								"    pm.expect([400, 422]).to.include(pm.response.code);",
								"});"
							]
						}
					}]
				}
{{- /* NOTE: "namespace пуст" test not applicable to AGL — namespace is set via URL path, not body */ -}}
			]
		},
		{
			"name": "{{ $res.Name }} — DELETE",
			"item": [
				{
					"name": "Удаление по name",
					"request": {
						"method": "DELETE",
						"header": [],
						"url": {
{{- if $res.IsNamespaced }}
							"raw": "{{`{{k8s_url}}`}}/apis/{{ $.Project.APIGroup }}/{{ $.Project.APIVersion }}/namespaces/{{`{{test_namespace}}`}}/{{ $plural }}/{{ $kebabName }}-new",
							"host": ["{{`{{k8s_url}}`}}"],
							"path": ["apis", "{{ $.Project.APIGroup }}", "v1alpha1", "namespaces", "{{`{{test_namespace}}`}}", "{{ $plural }}", "{{ $kebabName }}-new"]
{{- else }}
							"raw": "{{`{{k8s_url}}`}}/apis/{{ $.Project.APIGroup }}/{{ $.Project.APIVersion }}/{{ $plural }}/{{ $kebabName }}-new",
							"host": ["{{`{{k8s_url}}`}}"],
							"path": ["apis", "{{ $.Project.APIGroup }}", "v1alpha1", "{{ $plural }}", "{{ $kebabName }}-new"]
{{- end }}
						}
					},
					"event": [{
						"listen": "test",
						"script": {
							"type": "text/javascript",
							"exec": [
								"pm.test('Status code is 200', () => { pm.response.to.have.status(200); });"
							]
						}
					}]
				},
				{
					"name": "GET удалённого {{ $singular }} — 404",
					"request": {
						"method": "GET",
						"header": [],
						"url": {
{{- if $res.IsNamespaced }}
							"raw": "{{`{{k8s_url}}`}}/apis/{{ $.Project.APIGroup }}/{{ $.Project.APIVersion }}/namespaces/{{`{{test_namespace}}`}}/{{ $plural }}/{{ $kebabName }}-new",
							"host": ["{{`{{k8s_url}}`}}"],
							"path": ["apis", "{{ $.Project.APIGroup }}", "v1alpha1", "namespaces", "{{`{{test_namespace}}`}}", "{{ $plural }}", "{{ $kebabName }}-new"]
{{- else }}
							"raw": "{{`{{k8s_url}}`}}/apis/{{ $.Project.APIGroup }}/{{ $.Project.APIVersion }}/{{ $plural }}/{{ $kebabName }}-new",
							"host": ["{{`{{k8s_url}}`}}"],
							"path": ["apis", "{{ $.Project.APIGroup }}", "v1alpha1", "{{ $plural }}", "{{ $kebabName }}-new"]
{{- end }}
						}
					},
					"event": [{
						"listen": "test",
						"script": {
							"type": "text/javascript",
							"exec": [
								"pm.test('Status code is 404', () => { pm.response.to.have.status(404); });"
							]
						}
					}]
				},
				{
					"name": "Ошибка, name не существует",
					"request": {
						"method": "DELETE",
						"header": [],
						"url": {
{{- if $res.IsNamespaced }}
							"raw": "{{`{{k8s_url}}`}}/apis/{{ $.Project.APIGroup }}/{{ $.Project.APIVersion }}/namespaces/{{`{{test_namespace}}`}}/{{ $plural }}/nonexistent-{{ $kebabName }}",
							"host": ["{{`{{k8s_url}}`}}"],
							"path": ["apis", "{{ $.Project.APIGroup }}", "v1alpha1", "namespaces", "{{`{{test_namespace}}`}}", "{{ $plural }}", "nonexistent-{{ $kebabName }}"]
{{- else }}
							"raw": "{{`{{k8s_url}}`}}/apis/{{ $.Project.APIGroup }}/{{ $.Project.APIVersion }}/{{ $plural }}/nonexistent-{{ $kebabName }}",
							"host": ["{{`{{k8s_url}}`}}"],
							"path": ["apis", "{{ $.Project.APIGroup }}", "v1alpha1", "{{ $plural }}", "nonexistent-{{ $kebabName }}"]
{{- end }}
						}
					},
					"event": [{
						"listen": "test",
						"script": {
							"type": "text/javascript",
							"exec": [
								"pm.test('Status code is 404', () => { pm.response.to.have.status(404); });"
							]
						}
					}]
				}
{{- if $res.IsNamespaced }},
				{
					"name": "Ошибка, namespace не существует",
					"request": {
						"method": "DELETE",
						"header": [],
						"url": {
							"raw": "{{`{{k8s_url}}`}}/apis/{{ $.Project.APIGroup }}/{{ $.Project.APIVersion }}/namespaces/nonexistent-ns/{{ $plural }}/{{ $kebabName }}-0",
							"host": ["{{`{{k8s_url}}`}}"],
							"path": ["apis", "{{ $.Project.APIGroup }}", "v1alpha1", "namespaces", "nonexistent-ns", "{{ $plural }}", "{{ $kebabName }}-0"]
						}
					},
					"event": [{
						"listen": "test",
						"script": {
							"type": "text/javascript",
							"exec": [
								"pm.test('Status code is 404', () => { pm.response.to.have.status(404); });"
							]
						}
					}]
				}
{{- end }}
			]
		}
{{- end }}
{{- end }}
	],
	"variable": [
		{"key": "k8s_url", "value": "http://127.0.0.1:8001", "type": "default"},
		{"key": "test_namespace", "value": "tenant-0", "type": "default"}
	]
}
