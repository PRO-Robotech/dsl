{{- /* Code generated by sgctl. DO NOT EDIT. */ -}}
# ─── sgroups-migration (goose) ─────────────────────────────────
#
# Usage:  docker build -f build/docker/migration.Dockerfile -t sgroups-migration .
#

FROM golang:1.24-alpine AS toolchain
RUN go install github.com/pressly/goose/v3/cmd/goose@v3.24.1

FROM alpine:3.21
RUN apk add --no-cache ca-certificates tzdata
COPY --from=toolchain /go/bin/goose /usr/local/bin/goose

WORKDIR /migrations
COPY migrations/ /migrations/

ENV GOOSE_DRIVER=postgres
ENV GOOSE_DBSTRING="host=sgroups-postgres user=user_admin password=admin dbname={{ .DBSchema }} sslmode=disable"

ENTRYPOINT ["goose"]
CMD ["up"]
