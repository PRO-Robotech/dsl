// Code generated by sgctl. DO NOT EDIT.

package {{ $.DomainPkg }}

import (
	"time"

	"github.com/google/uuid"
)

{{ range $res := .CompositeResources }}
// {{ $res.Name }}SubType identifies a {{ $res.Name }} subtype.
type {{ $res.Name }}SubType string

const (
{{- range $res.Subtypes }}
	{{ $res.Name }}SubType{{ .Name }} {{ $res.Name }}SubType = "{{ .Name }}"
{{- end }}
)

// All{{ $res.Name }}SubTypes returns all subtypes of {{ $res.Name }}.
func All{{ $res.Name }}SubTypes() []{{ $res.Name }}SubType {
	return []{{ $res.Name }}SubType{
{{- range $res.Subtypes }}
		{{ $res.Name }}SubType{{ .Name }},
{{- end }}
	}
}

// {{ $res.Name }}Metadata holds common metadata for all {{ $res.Name }} subtypes.
type {{ $res.Name }}Metadata struct {
	UID              uuid.UUID             `json:"uid"`
	Name             string                `json:"name"`
	Namespace        string                `json:"namespace"`
	SubType          {{ $res.Name }}SubType `json:"sub_type"`
	DisplayName      string                `json:"display_name"`
	Description      string                `json:"description"`
	Comment          string                `json:"comment"`
	Labels           map[string]string     `json:"labels"`
	Annotations      map[string]string     `json:"annotations"`
	ResourceVersion  int64                 `json:"resource_version"`
	CreationTimestamp time.Time            `json:"creation_timestamp"`
}

// {{ $res.Name }} is the domain object for the composite resource {{ $res.Name }}.
type {{ $res.Name }} struct {
	{{ $res.Name }}Metadata
}

// {{ $res.Name }}Event represents a Watch event for {{ $res.Name }}.
type {{ $res.Name }}Event struct {
	Type     ResourceEventType `json:"type"`
	Object   *{{ $res.Name }}  `json:"object"`
}

// {{ $res.Name }}List is a collection of {{ $res.Name }} objects.
type {{ $res.Name }}List struct {
	Items []{{ $res.Name }} `json:"items"`
}
{{ end }}
