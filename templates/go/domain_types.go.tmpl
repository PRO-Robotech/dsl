// Code generated by sgctl. DO NOT EDIT.

package {{ $.DomainPkg }}

import "net"

// ResourceEventType represents the type of a watch event.
type ResourceEventType string

const (
	EventAdded    ResourceEventType = "ADDED"
	EventModified ResourceEventType = "MODIFIED"
	EventDeleted  ResourceEventType = "DELETED"
	EventInit     ResourceEventType = "INIT"
)

// Proto is the transport protocol enum.
type Proto string

const (
	ProtoTCP  Proto = "TCP"
	ProtoUDP  Proto = "UDP"
	ProtoICMP Proto = "ICMP"
)

// IPFamily represents IPv4/IPv6.
type IPFamily string

const (
	IPv4 IPFamily = "IPv4"
	IPv6 IPFamily = "IPv6"
)

// PolicyAction is the default action for an address group.
type PolicyAction string

const (
	PolicyActionDENY  PolicyAction = "DENY"
	PolicyActionALLOW PolicyAction = "ALLOW"
)

// IPNet wraps net.IPNet for domain use.
type IPNet struct {
	net.IPNet
}

// ParseIPNet parses a CIDR string into IPNet.
func ParseIPNet(s string) IPNet {
	_, n, err := net.ParseCIDR(s)
	if err != nil || n == nil {
		return IPNet{}
	}
	return IPNet{IPNet: *n}
}

// DualStackIPs holds IPv4 and IPv6 addresses.
type DualStackIPs struct {
	IPv4 []string `json:"IPv4"`
	IPv6 []string `json:"IPv6"`
}

// HostInfo holds OS/platform metadata for a host.
type HostInfo struct {
	HostName        string `json:"hostName"`
	OS              string `json:"os"`
	Platform        string `json:"platform"`
	PlatformFamily  string `json:"platformFamily"`
	PlatformVersion string `json:"platformVersion"`
	KernelVersion   string `json:"kernelVersion"`
}

// TransportEntry represents a single port range or ICMP type set.
type TransportEntry struct {
	Description string `json:"description"`
	Comment     string `json:"comment"`
	Ports       string `json:"ports,omitempty"`
	Types       []int  `json:"types,omitempty"`
}

// TransportSpec describes a protocol + IP family + entries.
type TransportSpec struct {
	Protocol Proto            `json:"protocol"`
	IPv      IPFamily         `json:"IPv"`
	Entries  []TransportEntry `json:"entries"`
}
