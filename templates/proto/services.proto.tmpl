// Code generated by sgctl. DO NOT EDIT.

syntax = "proto3";

package sgroups.v1;

option go_package = "{{ .Module }}/pkg/api/sgroups/v1";

import "sgroups/v1/domains_gen.proto";
import "sgroups/v1/queries_gen.proto";
import "google/api/annotations.proto";

{{ range $cr := .CompositeResources }}
// {{ $cr.Name }}Service provides CRUD and Watch operations for {{ $cr.Name }}.
service {{ $cr.Name }}Service {
  rpc List{{ $cr.Name }}s({{ $cr.Name }}Req_List) returns ({{ $cr.Name }}Resp_List) {
    option (google.api.http) = {
      post: "/v2/{{ httpPathComposite $cr }}/list"
      body: "*"
    };
  }

  rpc Upsert{{ $cr.Name }}s({{ $cr.Name }}Req_Upsert) returns ({{ $cr.Name }}Resp_Upsert) {
    option (google.api.http) = {
      post: "/v2/{{ httpPathComposite $cr }}/upsert"
      body: "*"
    };
  }

  rpc Delete{{ $cr.Name }}s({{ $cr.Name }}Req_Delete) returns ({{ $cr.Name }}Resp_Delete) {
    option (google.api.http) = {
      post: "/v2/{{ httpPathComposite $cr }}/delete"
      body: "*"
    };
  }

  rpc Watch{{ $cr.Name }}s({{ $cr.Name }}Req_Watch) returns (stream {{ $cr.Name }}Event) {
    option (google.api.http) = {
      post: "/v2/{{ httpPathComposite $cr }}/watch"
      body: "*"
    };
  }
}

{{ end }}

{{ range $res := .Resources }}
// {{ $res.Name }}Service provides CRUD and Watch operations for {{ $res.Name }}.
service {{ $res.Name }}Service {
  rpc List{{ $res.Name }}s({{ $res.Name }}Req_List) returns ({{ $res.Name }}Resp_List) {
    option (google.api.http) = {
      post: "/v2/{{ httpPathRes $res }}/list"
      body: "*"
    };
  }

  rpc Upsert{{ $res.Name }}s({{ $res.Name }}Req_Upsert) returns ({{ $res.Name }}Resp_Upsert) {
    option (google.api.http) = {
      post: "/v2/{{ httpPathRes $res }}/upsert"
      body: "*"
    };
  }

  rpc Delete{{ $res.Name }}s({{ $res.Name }}Req_Delete) returns ({{ $res.Name }}Resp_Delete) {
    option (google.api.http) = {
      post: "/v2/{{ httpPathRes $res }}/delete"
      body: "*"
    };
  }

  rpc Watch{{ $res.Name }}s({{ $res.Name }}Req_Watch) returns (stream {{ $res.Name }}Event) {
    option (google.api.http) = {
      post: "/v2/{{ httpPathRes $res }}/watch"
      body: "*"
    };
  }
{{- range .ExtraGRPCMethods }}

  rpc {{ .Name }}({{ $res.Name }}Req_{{ .Name }}) returns ({{ $res.Name }}Resp_{{ .Name }}) {
    option (google.api.http) = {
      post: "/v2/{{ httpPathRes $res }}/{{ .HTTPPath }}"
      body: "*"
    };
  }
{{- end }}
}

{{ end }}
